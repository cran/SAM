% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sam.R
\name{sam}
\alias{sam}
\title{Unified Entry Point for Sparse Additive Modelling}
\usage{
sam(X, y, p = 3, family = c("gaussian", "binomial", "poisson", "hinge"), ...)
}
\arguments{
\item{X}{Numeric training matrix with \code{n} rows (samples) and \code{d}
columns (features).}

\item{y}{Response vector of length \code{n}.}

\item{p}{The number of basis spline functions. The default value is 3.}

\item{family}{A string specifying the loss family. One of \code{"gaussian"}
(default), \code{"binomial"}, \code{"poisson"}, or \code{"hinge"}.}

\item{\dots}{Additional arguments passed to the family-specific function.}
}
\value{
An S3 object of class \code{samQL}, \code{samLL}, \code{samEL}, or
  \code{samHL}, depending on the chosen family.
}
\description{
Fit a sparse additive model by dispatching to the appropriate family-specific
function (\code{samQL}, \code{samLL}, \code{samEL}, or \code{samHL}).
}
\examples{
n <- 100; d <- 50
X <- matrix(runif(n * d), n, d)
y <- rnorm(n)
fit <- sam(X, y, family = "gaussian")
fit
}
\seealso{
\code{\link{samQL}}, \code{\link{samLL}}, \code{\link{samEL}},
  \code{\link{samHL}}
}
